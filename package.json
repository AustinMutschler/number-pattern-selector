{
  "name": "number-pattern-selector",
  "version": "1.0.1",
  "description": "NPS is a standardized number selector pattern. It allows for returning numbers based on a text pattern.",
  "author": "Austin Mutschler",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AustinMutschler/number-pattern-selector.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "@types/node": "^20.10.0",
    "jest": "^29.5.10",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.2"
  },
  "scripts": {
    "prepublish": "yarn build",
    "build": "rm -rf dist && tsc",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "keywords": [
    "nps",
    "number regex",
    "number pattern selector",
    "pattern"
  ]
}